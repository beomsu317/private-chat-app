object DaggerHilt {
    const private val hiltVersion = "2.38.1"
    const val hiltAndroid = "com.google.dagger:hilt-android:${hiltVersion}"
    const val hiltCompiler = "com.google.dagger:hilt-compiler:${hiltVersion}"

    const private val navigationVersion = "1.0.0"
    const val hiltNavigationCompose = "androidx.hilt:hilt-navigation-compose:${navigationVersion}"
}